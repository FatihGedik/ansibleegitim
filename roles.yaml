---

- hosts: databases
  become: true
  remote_user: badmin
  roles: 
    - role: postgresql
  tags: psql

- hosts: all
  become: true
  remote_user: badmin
  roles:
    - role: common
  tags: com

- hosts: webservers
  become: true
  remote_user: badmin
  roles:
   - role: mrlesmithjr.apache2
   - role: apache2modify
  tags: web

- hosts: loadbalancers
  become: true
  remote_user: badmin
  roles:
    - role: debug
      me: 'roles.yaml'
  tags: deb

- hosts: databases
  become: yes
  remote_user: badmin
  vars_files:
    - vars/main.yml
  roles:
    - role: geerlingguy.mysql 
  tags: mysql

